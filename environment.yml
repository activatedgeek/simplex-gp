name: bgp
channels:
  - conda-forge
dependencies:
  - ninja
  - python=3.7
  - pip
  - pip:
    - jupyterlab
    - ipywidgets
    - numpy
    - scipy
    - scikit-learn
    - seaborn
    - tqdm
    - fire
    - tensorboard
    - torch
    - gpytorch
    - wandb
